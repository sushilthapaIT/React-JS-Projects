to create react app => npm create vite@latest
to run => npm run dev


to install react icon => npm install react-icons --save

to install tailwindcss in react => npm install -D tailwindcss postcss autoprefixer
                                => npx tailwindcss init -p

==> add extension tailwind css it will make easier to write 
==> add prettier(extension) it will make the order same
        => npm install -D prettier prettier-plugin-tailwindcss