import styled from "styled-components"

const Navbar = () => {
    return(
        <>
            <Nav>

            </Nav>
        </>
    );
}

export default Navbar

const Nav = styled.nav`
    position: sticky;
`